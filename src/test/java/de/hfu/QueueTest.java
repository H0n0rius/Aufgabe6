package de.hfu;

import org.junit.Test;
import org.junit.Before;
import static org.junit.Assert.*;
/*
public class QueueTest {

	private Queue que;
	private int len = 3;
	
	@Before
	public void erzeugeQueue() {
		que = new Queue(len);
	}
	
	@Test
	public void testEnqueue() {
		for(int i = 0; i < 3; ++i) {
			que.enqueue(i);
			assertEquals("i= " + i, i, que.dequeue());
		}
		for(int i = 0; i < 7; ++i) {
			que.enqueue(i);
		}
		assertEquals("letztes in Queue muss 0 sein", 0, que.dequeue());
		assertEquals("letztes in Queue muss 1 sein", 1, que.dequeue());
		assertEquals("letztes in Queue muss 6 sein", 6, que.dequeue());
	}

	@Test
	public void testDequeue() {
		try{
			que.dequeue();
			fail("die Queue ist leer");
		}catch(Exception ex) {
			
		}
		
			
	}

}
*/