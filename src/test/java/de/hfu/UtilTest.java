package de.hfu;

import org.junit.Test;
//import org.junit.Before;
import static org.junit.Assert.*;
/*
public class UtilTest {
	
	@Test
	public void testIstErstesHalbjahr() {
		//assertEquals("0 ist im ersten Halbjahr", new IllegalArgumentException(), Util.istErstesHalbjahr(0));	

		assertEquals("1 ist im ersten Halbjahr", true, Util.istErstesHalbjahr(1));
		assertEquals("6 ist im ersten Halbjahr", true, Util.istErstesHalbjahr(6));
		assertEquals("7 ist im ersten Halbjahr", false, Util.istErstesHalbjahr(7));
		assertEquals("12 ist nicht im ersten Halbjahr", false, Util.istErstesHalbjahr(12));
		
		//assertEquals("13 sollte Fehler werfen", new IllegalArgumentException(), Util.istErstesHalbjahr(13));	
		
	}
}
*/